import SwiftUI

extension Color {
    static let appAccent = Color("AppAccent")
    static let appStatusUp = Color("StatusUp")
    static let appStatusDegraded = Color("StatusDegraded")
    static let appStatusDown = Color("StatusDown")
    static let appStatusOffline = Color("StatusOffline")

    static let kumaGreen = Color(red: 0.36, green: 0.87, blue: 0.55)
    static let kumaGreenLight = Color(red: 0.55, green: 0.94, blue: 0.72)
    static let kumaGreenDim = Color(red: 0.20, green: 0.50, blue: 0.32)
}
